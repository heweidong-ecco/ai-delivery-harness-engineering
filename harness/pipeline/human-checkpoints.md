# 人工确认点

## 五个确认点

| 编号 | 名称             | 时机         | 决策权 | 确认人 | 确认内容           | 未确认处理 |
| ---- | ---------------- | ------------ | ------ | ------ | ------------------ | ---------- |
| HC-1 | 需求待决议确认   | 需求评审后   | 人工   | <角色> | 边界、验收、优先级 | 阻塞       |
| HC-2 | 计划评审后确认   | 计划评审后   | 人工   | <角色> | 任务、依赖、风险   | 阻塞       |
| HC-3 | 编码评审后确认   | 编码评审后   | 人工   | <角色> | 代码质量、业务逻辑 | 阻塞       |
| HC-4 | 部署环境参数确认 | 部署前       | 人工   | <角色> | 环境、配置、参数   | 阻塞       |
| HC-5 | 最终交付确认     | 用户确认阶段 | 人工   | <角色> | 是否满足需求       | 阻塞       |

## 确认记录

每次确认写入 `harness/checkpoints/<REQ>-HC<N>.md`(格式见 `harness/checkpoints/README.md`)。
该路径是**权威源**:`harness/state/stages.json` 里阶段 10「用户确认」的产出物即按此定义。

## 硬性约束

- 未确认不得进入下一阶段。
- Agent 不得代替人工确认。
- 确认人不得是编码 Agent。
- 确认记录必须留痕。

> ⚠ **如实标注(空跑-3)**:上面第一条「未确认不得进入下一阶段」目前**只有 HC-5 有机制**。
> `harness/state/stages.json` 只把 `harness/checkpoints/<REQ>-HC5.md` 列为阶段 10 的产出物;
> **HC-1 ~ HC-4 完全是文字约定,没有任何门禁拦它们** —— 与本仓修过的 H4/H5 同类:
> 写下来的约束没有机制。属已知缺口,尚未处理(见 `harness/pilot/findings.md`)。
